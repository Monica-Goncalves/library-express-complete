<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Classroom Library</title>
  <link rel="shortcut icon" href="favicon.ico">
  <link rel="stylesheet" href="font-awesome.min.css">
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>Classroom Library</h1>
  <h2>check in & return your books here, so your classmates know what books are availabe</h2>


  <h3>Library</h3>
  <ul class="messages">
  <% for(var i=0; i<library.length; i++) {%>
    <li class="message">

      <% if (library[i].checkedOut === true){ %>
        <span class="strikeout"><%= library[i].title %></span>
        <span class="strikeout"><%= library[i].author %></span>

      <% }else{ %>
        <span><%= library[i].title %></span>
        <span><%= library[i].author %></span>
      <% } %>
      <span><i class="fa fa-check-square" aria-hidden="true"></i></span>
      <span><i class="fas fa-home" aria-hidden="true"></i></span>
      <span><i class="fa fa-trash" aria-hidden="true"></i></span>

    </li>
  <% } %>
  </ul>

  <h2>Add a message</h2>

  <form action="/library" method="POST">
    <input type="text" placeholder="title" name="title">
    <input type="text" placeholder="author" name="author">
    <button type="submit">Submit</button>
  </form>
  <script src="https://kit.fontawesome.com/c0e668a8ad.js" crossorigin="anonymous"></script>
  <script src="main.js"></script>

</body>
</html>
